{% extends 'base.html.twig' %}

{% block body %}

    <h1>Proposer une nouvelle sortie</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-secondary">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}

{% block title %}
    Proposer une sortie | {{ parent() }}
{% endblock %}

